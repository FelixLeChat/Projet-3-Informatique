namespace FrontEnd.Game
{
    /// <summary>
    /// Some enum need for the configuration of a game
    /// </summary>
    public enum PlayerCountMode
    {
        Solo = 1,
        Coop = 2
    }

    public enum PlayerType
    {
        Human,
        Computer
    }

    public enum ZoneConfig
    {
        SoloMap,
        MultiMap
    }

    public enum MultiMode
    {
        Coop,
        Competition
    }
}